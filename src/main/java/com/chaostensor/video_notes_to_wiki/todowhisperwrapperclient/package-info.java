/**
 * TODO I need a multi module for whisper so that I can get a lightweight client.
 *
 *   Those CAN Be dangerous though as they have java dependencies which may not match.
 *
 *   SO a better srat would be to get a json schema and open api doc from the service,
 *
 *   and code generate a client here. Still though as a separate module but here in this repo.
 *
 *   Code gen that and then consume it int he web client.
 *
 *   For now though, duplicating these models.
 */
package com.chaostensor.video_notes_to_wiki.todowhisperwrapperclient;